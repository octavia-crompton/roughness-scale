"""Insert slope-integrated prediction cells after the fig6 CF plot cell."""
import json
from pathlib import Path

NB = Path("/Users/octaviacrompton/Projects/roughness-scale/notebooks/4. roughness_scale-cf-channel_eqs.ipynb")
with open(NB) as f:
    nb = json.load(f)

# The fig6 plot cell is at index 11.  Insert after it.
INSERT_IDX = 12

NEW_MD = {
    "cell_type": "markdown",
    "metadata": {},
    "source": [
        "### Slope-integrated predictions\n",
        "\n",
        "The CF approach uses the full slope length $l$ at every cell, ignoring each cell's actual position.\n",
        "The **slope-integrated** version integrates the kinematic-wave formula along each column using the correct upslope distance $x$,\n",
        "then spatially averages \u2014 capturing flow accumulation effects.\n",
        "\n",
        "For the perturbation expansion, instead of CF substitutions ($\\upsilon \\approx -\\tfrac{3}{5}r$, $\\eta \\approx \\tfrac{3}{5}r$),\n",
        "we use the **actual simulation covariances** $\\langle r^2\\rangle$, $\\langle \\upsilon^2\\rangle$, $\\langle r\\upsilon\\rangle$,\n",
        "$\\langle r\\eta\\rangle$, $\\langle \\eta\\upsilon\\rangle$, $\\langle \\eta^2\\rangle$ computed from the spatial fields at equilibrium.\n",
        "This isolates whether the CF error comes from the spatial-averaging *method* (CF vs slope-integrated)\n",
        "or from the CF *substitution* (replacing covariances with functions of $\\langle r^2\\rangle$).\n",
    ],
}

NEW_CODE = {
    "cell_type": "code",
    "metadata": {},
    "outputs": [],
    "execution_count": None,
    "source": [
        "# \u2500\u2500 Slope-integrated predictions: Leading term + actual-covariance expansion \u2500\u2500\n",
        "# Replaces CF fields with slope-integrated <ha>, <Ua>;\n",
        "# replaces CF substitutions with actual simulation covariances.\n",
        "\n",
        "# \u2500\u2500 Leading term (slope-integrated): exact back-calculation \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n",
        "summary['er_lead_int'] = (\n",
        "    summary['<ha>']**(2/3) * summary['So']**0.5\n",
        "    / (summary['<Ua>'] * summary['<n>'])\n",
        ")\n",
        "\n",
        "# \u2500\u2500 T\u2080 (actual covariances): r, \u03c5 terms only \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n",
        "summary['er_T0_actual'] = (\n",
        "    1\n",
        "    + summary['<r2>']                        # \u27e8r\u00b2\u27e9\n",
        "    + summary['<ups2>']                      # \u27e8\u03c5\u00b2\u27e9  (actual, not (3/5)\u00b2\u27e8r\u00b2\u27e9)\n",
        "    + 4 * summary['<r ups>']                 # 4\u27e8r\u03c5\u27e9  (actual, not -12/5\u27e8r\u00b2\u27e9)\n",
        ")\n",
        "\n",
        "# \u2500\u2500 Full 2nd-order (actual covariances): all six terms \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n",
        "summary['er_T2_actual'] = (\n",
        "    1\n",
        "    + summary['<r2>']                        # \u27e8r\u00b2\u27e9\n",
        "    + summary['<ups2>']                      # \u27e8\u03c5\u00b2\u27e9\n",
        "    + 4 * summary['<r ups>']                 # 4\u27e8r\u03c5\u27e9\n",
        "    - (8/3) * summary['<r eta>']             # -8/3 \u27e8r\u03b7\u27e9\n",
        "    - (8/3) * summary['<eta ups>']           # -8/3 \u27e8\u03b7\u03c5\u27e9\n",
        "    + (14/9) * summary['<eta2>']             # 14/9 \u27e8\u03b7\u00b2\u27e9\n",
        ")\n",
        "\n",
        "# \u2500\u2500 method list \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n",
        "methods_int = [\n",
        "    ('er_lead_int',   'Leading term (slope-integrated)'),\n",
        "    ('er_T0_actual',  r'$T_0$: actual covariances'),\n",
        "    ('er_T2_actual',  r'Full 2nd-order: actual covariances'),\n",
        "]\n",
        "\n",
        "# \u2500\u2500 1\u00d73 plot (reuse colour scheme from CF cell above) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n",
        "from matplotlib.ticker import MaxNLocator\n",
        "from matplotlib.lines import Line2D\n",
        "\n",
        "fig_int, axes_int = plt.subplots(1, 3, figsize=(15, 5), sharey=True)\n",
        "\n",
        "for idx, (ax, (pred_col, title)) in enumerate(zip(axes_int.ravel(), methods_int)):\n",
        "    obs  = summary[er_obs_col].to_numpy(float)\n",
        "    pred = summary[pred_col].to_numpy(float)\n",
        "    finite = np.isfinite(obs) & np.isfinite(pred)\n",
        "    if finite.sum() == 0:\n",
        "        ax.set_title(f\"{title}\\n(no data)\", fontsize=fs_lab)\n",
        "        continue\n",
        "\n",
        "    lo = min(np.nanmin(obs[finite]), np.nanmin(pred[finite]))\n",
        "    hi = max(np.nanmax(obs[finite]), np.nanmax(pred[finite]))\n",
        "    ax.plot([lo, hi], [lo, hi], 'k-', lw=1, zorder=0)\n",
        "\n",
        "    _rng = np.random.default_rng(42)\n",
        "    for cv in _color_levels:\n",
        "        sel = finite & (np.abs(_color_vals - cv) < 1e-9)\n",
        "        if sel.sum() == 0:\n",
        "            continue\n",
        "        _jitter = _rng.normal(0, 0.005, sel.sum())\n",
        "        ax.scatter(obs[sel], pred[sel] + _jitter,\n",
        "                   color=_palette_4p[cv], marker='o',\n",
        "                   s=25, alpha=0.80, edgecolors='none', zorder=3)\n",
        "\n",
        "    rmse = _rmse(pred[finite], obs[finite])\n",
        "    r2v  = _r2(pred[finite], obs[finite])\n",
        "    ax.set_title(title, fontsize=fs_lab)\n",
        "    ax.text(0.97, 0.05, f'RMSE={rmse:.3f}\\n$R^2$={r2v:.2f}',\n",
        "            transform=ax.transAxes, fontsize=fs_tck - 1,\n",
        "            ha='right', va='bottom', zorder=5)\n",
        "    ax.tick_params(labelsize=fs_tck)\n",
        "    ax.grid(True, alpha=0.2)\n",
        "    ax.xaxis.set_major_locator(MaxNLocator(nbins=4))\n",
        "    ax.yaxis.set_major_locator(MaxNLocator(nbins=4))\n",
        "    ax.set_xlabel(r\"observed $n_e/\\langle n\\rangle$\", fontsize=fs_lab)\n",
        "    if idx == 0:\n",
        "        ax.set_ylabel(r\"predicted $n_e/\\langle n\\rangle$\", fontsize=fs_lab)\n",
        "\n",
        "fig_int.suptitle(r\"Slope-integrated & actual-covariance predictions of $n_e/\\langle n\\rangle$\"\n",
        "                 f\"  \u2014  coloured by {COLOR_BY_LABEL_4P}\", fontsize=fs_lab + 1)\n",
        "fig_int.tight_layout(rect=[0, 0, 0.84, 1])\n",
        "\n",
        "leg_ax_int = fig_int.add_axes([0.85, 0.12, 0.13, 0.74])\n",
        "leg_ax_int.set_axis_off()\n",
        "_leg_int = [Line2D([0], [0], marker='o', color='w',\n",
        "                   markerfacecolor=_palette_4p[cv], markersize=8, label=_level_label(cv))\n",
        "            for cv in _color_levels]\n",
        "leg_ax_int.legend(handles=_leg_int, title=COLOR_BY_LABEL_4P, loc='center left',\n",
        "                  fontsize=fs_leg, title_fontsize=fs_leg, frameon=True, handlelength=1.2)\n",
        "\n",
        "# \u2500\u2500 save to scratch \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n",
        "_, _scratch, _ = _fig_dirs()\n",
        "fig_int.savefig(_os.path.join(_scratch, 'scratch_slope_integrated_3panel.png'),\n",
        "                dpi=200, bbox_inches='tight')\n",
        "\n",
        "# \u2500\u2500 comparison table: CF vs slope-integrated \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n",
        "print(f\"{'Method':<42}  {'RMSE':>7}  {'R\\u00b2':>6}\")\n",
        "print('-' * 60)\n",
        "_all_methods = list(methods) + list(methods_int)\n",
        "for _pc, _ttl in _all_methods:\n",
        "    _p = summary[_pc].to_numpy(float)\n",
        "    _o = summary[er_obs_col].to_numpy(float)\n",
        "    _f = np.isfinite(_p) & np.isfinite(_o)\n",
        "    if _f.sum() < 3:\n",
        "        print(f\"{_ttl:<42}  {'n/a':>7}  {'n/a':>6}\")\n",
        "        continue\n",
        "    _rv = _r2(_p[_f], _o[_f])\n",
        "    _rm = _rmse(_p[_f], _o[_f])\n",
        "    print(f\"{_ttl:<42}  {_rm:7.4f}  {_rv:6.3f}\")\n",
        "\n",
        "plt.show()\n",
    ],
}

nb["cells"].insert(INSERT_IDX, NEW_MD)
nb["cells"].insert(INSERT_IDX + 1, NEW_CODE)

with open(NB, "w") as f:
    json.dump(nb, f, indent=1, ensure_ascii=False)
    f.write("\n")

print(f"Inserted markdown + code cells at indices {INSERT_IDX}, {INSERT_IDX+1}")
print(f"Notebook now has {len(nb['cells'])} cells")
